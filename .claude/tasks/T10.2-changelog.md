# T10.2 — `CHANGELOG.md`

**Phase** P10 — Docs & the artifact · **Board** [`.claude/TASKS.md`](../TASKS.md) (status lives there)

## Goal

A `0.1.0` entry that reads as a description of what the package does, not as a git log.

## Acceptance criteria

- Keep a Changelog format, semver headings, dated `0.1.0` entry.
- Lists the public subpaths shipped in 0.1.0 — this is the release's actual API surface.
- Calls out the deliberate constraints a consumer will hit: **React 19+ only** (§ A8),
  ESM-only, no root entry, zero runtime dependencies, one collision strategy.
- Version matches `package.json`.
- No entries for work that didn't ship.

## Files

- `CHANGELOG.md` (new)

## Where it stands

`CHANGELOG.md` on disk (2026-08-08). Keep a Changelog format, semver heading, dated `0.1.0`
entry matching `package.json`.

Written as a description of what the package **is**, not a git log — the fourteen subpaths that
make up the release's actual API surface, then the constraints a consumer will hit, each stated
as a decision with its reason rather than as a limitation: React 19+, ESM-only, no root entry,
zero runtime dependencies, one collision strategy, indicator-only tree mode, and the asymmetric
mid-drag removal policy.

The three consumer contracts and the explicit not-included list are there too, so someone reading
only the changelog knows what they are getting and what they are not.