# T0.4 — `.gitignore`

**Phase** P0 — Governance · **Board** [`.claude/TASKS.md`](../TASKS.md) (status lives there)

## Goal

Keep build output, dependencies, and local noise out of the repository.

## Test first

Not applicable — no code.

## Acceptance criteria

- Ignores `node_modules/`, `dist/`, `coverage/`, `*.tsbuildinfo`, `*.log`, `.DS_Store`,
  and `playground/dist/`.
- Does **not** ignore `bun.lock` — the lockfile is committed.
- Does **not** ignore `.claude/` — the task board is part of the repo, and ground rule 3
  points at it. Ignoring the directory wholesale would ship a `CLAUDE.md` referencing a
  board the repo doesn't contain.
- **Does** ignore `.claude/settings.local.json` specifically — that file holds
  machine-local tool permissions, not project state. This is the one exception to the
  line above, and the reason the ignore must be file-scoped rather than directory-scoped.

## Files

- `.gitignore`

## Where it stands

Done. Current contents cover everything above; revisit if bun's lockfile name or
Biome's cache location (`.biome/`) turns out to need an entry during [T1.4](T1.4-biome-and-vitest-config.md).
