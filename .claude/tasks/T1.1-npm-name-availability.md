# T1.1 — Confirm `fc-react-dnd` is free on npm

**Phase** P1 — Scaffold · **Board** [`.claude/TASKS.md`](../TASKS.md) (status lives there)

## Goal

Know the package name is claimable before it is baked into `package.json`, the
`exports` map, every import in the README, and the blog post.

## Test first

Not applicable — a registry lookup, no code.

## Acceptance criteria

- `npm view fc-react-dnd` returns 404 / "is not in this registry".
- If the name **is** taken: stop, report to the user, and do not pick a replacement
  unilaterally — the name appears in the public API of every example.
- Result recorded in "Where it stands" below with the date checked.

## Files

None (read-only check).

## Where it stands

Checked 2026-08-08 on npm 11.16.0: `npm view fc-react-dnd` returned `E404 — Not Found -
GET https://registry.npmjs.org/fc-react-dnd`. The name is free and safe to bake into
`package.json`, the `exports` map, the README examples, and the blog post.

Re-check before publishing — a 404 today is not a reservation, and the gap between now and
the user's publish decision is open-ended.
