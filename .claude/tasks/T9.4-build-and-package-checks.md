# T9.4 — Build output and package checks

**Phase** P9 — Integration & verification · **Board** [`.claude/TASKS.md`](../TASKS.md) (status lives there)

## Goal

Prove the thing consumers install actually resolves. An ESM-only package with a
root-entry-less subpath `exports` map has exactly the shape that tooling gets wrong, so it
gets checked by machine rather than by reading the manifest.

## Acceptance criteria

- `bun run build` emits `dist/` mirroring `src/` 1:1 — `.js`, `.d.ts`, `.js.map`,
  `.d.ts.map` per module, and **no test files**.
- Every subpath in `package.json#exports` resolves to files that exist on disk.
- Nothing in `dist/` imports a package that isn't a peer dependency — zero runtime deps,
  verified rather than assumed.
- `'use client'` directives survive the `tsc` emit where they were in the source.
- `publint` clean.
- `attw --pack . --profile esm-only` clean — no false CJS, no masquerading types.
- **A deep import into `dist/internal/*` fails to resolve** from a consumer's perspective.
  This is ground rule 4 enforced by the module resolver; assert it, don't trust it.
- `bun run verify` green end to end.

## Files

- No new source files — this task verifies the output of the others. Any fix it forces
  lands in the task that owns the file.

## Where it stands

Verified 2026-08-08 against the emitted `dist/`. `bun run verify` green end to end; every
criterion above checked by machine rather than by reading the manifest.

| Check | Result |
| --- | --- |
| `dist/` mirrors `src/` 1:1 | 25 source modules → **100 files** (`.js`, `.js.map`, `.d.ts`, `.d.ts.map` each); none missing |
| Test files in the tarball | **none** — `tsconfig.build.json`'s exclude holds |
| `exports` subpaths | **14**, exactly the `CLAUDE.md` module map, no more and no fewer, `types` before `default` in every one |
| Export targets on disk | all resolve |
| Anything mapping into `dist/internal/` | none |
| Bare imports anywhere in `dist/` | `react`, `react-dom`, `react/jsx-runtime` — all peers. No `dependencies` field exists at all |
| Relative imports carrying `.js` | all of them |
| `'use client'` surviving the emit | 15 source modules have it, 15 emitted files have it |
| `publint` | `All good!` |
| `attw --profile esm-only` | clean on `node16 (from ESM)` and `bundler`; the `node10` and CJS rows are ignored by the profile, as intended for an ESM-only package |
| `dist/` size | 504 KB (with source maps and declaration maps) |

### Ground rule 4 is enforced by the module resolver, and it is asserted

Checked from a **consumer's** position — a scratch package with `node_modules/fc-react-dnd`
symlinked at it — using `import.meta.resolve`:

```
RESOLVES   fc-react-dnd/tree
RESOLVES   fc-react-dnd/use-tree-drop
BLOCKED    fc-react-dnd/internal/store          ERR_PACKAGE_PATH_NOT_EXPORTED
BLOCKED    fc-react-dnd/dist/internal/store.js  ERR_PACKAGE_PATH_NOT_EXPORTED
BLOCKED    fc-react-dnd                         No "exports" main defined
```

The last line is the no-root-entry rule holding: `import { useDraggable } from 'fc-react-dnd'`
does not fail in review, it fails at resolution.

### One false alarm worth recording

A first pass scanning `dist/**/*.js` for `from '…'` reported `fc-react-dnd/dnd-provider` and
`fc-react-dnd/sortable-list` as bare imports. They are not imports — they are inside the
out-of-provider **error messages**, which tell a consumer which module to import `DndProvider`
from. Confirmed by matching whole `import`/`export` statements instead of any `from '…'`.